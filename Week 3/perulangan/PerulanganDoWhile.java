package perulangan;

public class PerulanganDoWhile {
    public static void main(String[] args) {
        int maxStage = 2;

        do {
            System.out.println("Player di stage " + maxStage);
        } while (maxStage < 2);
    }
}
